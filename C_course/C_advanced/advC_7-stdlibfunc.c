//STANDARD LIBRARY FUNCTION
/* A standard library function is a predefined function 
in the C programming language that performs common tasks 
— so you don’t need to write your own code for things like 
input, output, math, string handling, etc. */
#include <stdio.h>

/* 1. MATH function
ex: sqrt()------#include <math.h>
ex: printf("Cube root of %lf", sqrt(num));
cbrt()---cube root

/* 2. CTYE HEADER
ex: #include <ctype.h>
char alpha = 'e'
char upper = toupper(alpha); ----- makes the 'e' to 'E'
printf("%c", upper);

this save a lot of time */


int main() {
    
    
    return 0;
}
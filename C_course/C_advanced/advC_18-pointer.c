#include <stdio.h>

int main() {
    
    int age = 25;
    // 1. Pointer variables
    //int* ptr = &age;
    //printf("%p", ptr);
    
    //2. Access value using pointer
    //int* ptr = &age;
    //printf("%d", *ptr);
    
    //3. Change value using pointer
    //int* ptr = &age;
    //*ptr = 31;
    //printf("%d", age); //---> 31
    //printf("%p", &age);
    //%p - used with printf to print memory addresses (pointers).
    return 0;
}